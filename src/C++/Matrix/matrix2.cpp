#include <iostream>
#include <vector>

template <typename T> struct Point {
  T x, y;
};

template <typename T> std::vector<Point<T>> grid(const T *max, const int *n) {
  std::vector<Point<T>> res;
  return res;
}

int main() {
  std::vector<Point<double>> points;
  return 0;
}
